def main(data:str):
    f=open('data01.txt')
    x=f.read()
    x=x.split()
    """
    The data is from the file. Return data as a list type.
    Args:
        data: str
    Returns:
        list: return answer
    """
    return x

# Read data from file